package lambdas;

import java.time.LocalDate;
import java.util.List;
import java.util.function.*;

class Student {
  double getGpa() {return 0;}
  List<String> getCourses() {return List.of();}
}
public class TestExamples {
//  Predicate<Student> sc = Student s -> { return s.getGpa() > 3;};
//  Predicate<Student> sc = s -> { s.getGpa() > 3 };
//  Object sc = s -> {return s.getGpa() > 3;};
//  Predicate<Student> sc = (s) -> s.getGpa() > 3;
//  Predicate<Student> sc = s -> s.getCourses().size();
//
//  Function<String, String> fss = s -> { System.out.println(s); };
//  Consumer<String> cs = s -> { System.out.println(s); };
//  Supplier<String> ss = (s1, s2) -> s1 + s2;
//  BiPredicate<String> bps = (String s) -> { return s.length() > 0; };
//  ToIntFunction<String> tofs = s -> s.length();

//  BiPredicate<String, LocalDate> bpsl = (s, l) -> l.

//    BiFunction<String, LocalDate, Integer> xxx = (s, l) -> l.
}
